[package]
name = "kngsterr-api"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.1"
dotenv = "0.15.0"
tokio = {version = "1.43.0", features = ["full"]}
tower-http = {version = "0.6.2" , features = ["cors"]}
